name: Select literal and data reference
category: [Tests]
runners.users: [alex]
runners.groups: [admins]

fields:
  - id: a
    kind: select
    source.literal: [a, b, c]
    optional.when: 10

  - id: b
    kind: mega

  - id: c
    kind: select
    source.fetch:
      url: asdf
      body.literal: 10
